# =============================================================
# Finance ITSC Dashboard — Environment Variables
# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าให้ครบก่อนรัน
# cp .env.example .env
# =============================================================

# -------------------------------------------------------------
# OpenAI API
# สร้าง API key ได้ที่ https://platform.openai.com/api-keys
# -------------------------------------------------------------
OPENAI_API_KEY=

# -------------------------------------------------------------
# Email Alerts (Airflow)
# Gmail: ต้องใช้ App Password ไม่ใช่รหัส Gmail ปกติ
# วิธีสร้าง App Password:
#   1. เปิด 2FA ที่ myaccount.google.com/security
#   2. ไปที่ Security → App Passwords
#   3. สร้าง App Password สำหรับ "Mail"
# -------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password


ETL_MAX_RETRIES=3
ETL_RETRY_DELAY=5

KEEP_VERSIONS=10

COOKIE_SECRET=your-random-secret-key-here